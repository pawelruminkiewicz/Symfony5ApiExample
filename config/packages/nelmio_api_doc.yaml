nelmio_api_doc:
    documentation:
        info:
            title: Documentation for exams system
            description: This documentation present api for exams system
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
        # host_patterns:
        #     - ^api\.
